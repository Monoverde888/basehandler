let config = {
    token: "Token de tu bot", // Reemplaze esto con el token de tu de bot.
    prefix: "Prefijo", // El prefix de su bot para indentificar sus comandos.
  
  }
  
  // Aqui exportamos el objeto config:
  module.exports = config;
  
  